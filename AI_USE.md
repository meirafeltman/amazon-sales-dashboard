# AI Use Log

Fill this in as you go — it should reflect what you actually asked for and actually did with the output, not a generic list. A few starter rows based on the initial build are below; edit or replace them with your own words once you've gone through the code yourself.

| Tool used | What I asked it to do | What I kept / changed / rejected, and why |
|---|---|---|
| Claude | Draft a first version of app.py that loads amazon.csv, cleans the price/rating/discount fields, and builds a Streamlit dashboard with metrics and charts | Kept the overall structure. Reviewed the cleaning logic line by line to confirm it matched what the assignment described (₹ and comma stripping, coercing the bad rating value, dropping unusable rows) before running it. |
| Claude | Explain why `errors="coerce"` was used on the `rating` column instead of a manual fix | Used this to understand the approach well enough to explain it in class, rather than just accepting the line as-is. |
| *(add your own rows here)* | | |

Add a new row every time you use an AI tool for a distinct task (fixing an error, adding a chart, cleaning up the README, etc.), even if it's a small one.
